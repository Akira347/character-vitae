# Example env file — fill locally / in CI / in your hosting secrets
APP_ENV=dev
APP_DEBUG=1
APP_SECRET=REPLACE_THIS_WITH_A_STRONG_SECRET

# Example database URL (replace)
DATABASE_URL="postgresql://postgres:password@localhost:5432/character_vitae?serverVersion=16&charset=utf8"

# JWT settings (do NOT put PEM contents here; use secrets)
# JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
# JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
# JWT_PASSPHRASE=REPLACE_WITH_PASSPHRASE (store as secret)

MAILER_DSN=smtp://user:pass@localhost:1025
